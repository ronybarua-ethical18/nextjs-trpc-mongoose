{
  "extends": [
    "next/core-web-vitals",
    "next/typescript",
    "plugin:prettier/recommended" // Enables Prettier rules and turns off conflicting ESLint rules
  ],
  "rules": {
    // Example of custom rules, you can add or modify them as needed
    "prettier/prettier": "error", // Treat Prettier formatting issues as ESLint errors
    "@typescript-eslint/no-unused-vars": "warn" // Optional: Customize TypeScript rules
  }
}
